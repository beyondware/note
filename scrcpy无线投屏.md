首次插上数据线（开发者模式——USB 调试）

```sh
adb tcpip 5555
```

拨掉数据线

设置——关于手机——状态信息——获取 IP 地址 192.168.1.8

```sh
adb connect 192.168.1.8:5555
```

（开发者模式——USB 调试），否则报错

```sh
“由于目标计算机积极拒绝，无法连接”错误
```

运行 scrcpy

```sh
scrcpy
```
